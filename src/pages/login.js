import React, { Component } from "react";

export class login extends Component {
  render() {
    console.log(login);
    return (
      <div>
        <h1>Login</h1>
      </div>
    );
  }
}

export default login;
